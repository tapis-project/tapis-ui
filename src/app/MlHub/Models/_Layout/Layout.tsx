import React from 'react';
import { Router } from '../_Router';
import SearchBar from 'app/MlHub/_components/SearchBar/SearchBar';

const Layout: React.FC = () => {
  return <Router />;
};

export default Layout;
