import React from 'react';
import { Router } from '../_Router';
import SearchBar from 'app/MlHub/_components/Search/Search';

const Layout: React.FC = () => {
  return <Router />;
};

export default Layout;
