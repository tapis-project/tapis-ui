import { AppsReducerState, AppsAction } from './types';
export declare const initialState: AppsReducerState;
export declare function apps(state: AppsReducerState, action: AppsAction): AppsReducerState;
