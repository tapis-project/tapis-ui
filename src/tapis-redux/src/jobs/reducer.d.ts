import { JobsReducerState, JobsAction } from './types';
export declare const initialState: JobsReducerState;
export declare function jobs(state: JobsReducerState, action: JobsAction): JobsReducerState;
