import { authenticatorStore as authenticator } from '../authenticator/authenticator.fixtures';
import { systemsStore as systems } from '../systems/systems.fixtures';

const tapisReduxStore = {
  authenticator,
  systems,
};

export default tapisReduxStore;
